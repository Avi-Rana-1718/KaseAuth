apiVersion: apps/v1
kind: Deployment
metadata:
  name: kase-auth
spec:
  replicas: 1
  selector:
    matchLabels:
      app: kase-auth
  template:
    metadata:
      labels:
        app: kase-auth
    spec:
      containers:
        - name: kase-auth
          image: kase-auth:latest
          imagePullPolicy: Never
          ports:
            - containerPort: 8080
          resources:
            requests:
              memory: 256Mi
              cpu: 250m
            limits:
              cpu: 500m
              memory: 512Mi